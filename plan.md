# DorisMan — 功能执行计划清单（待确认版）

> 目标：面向 Apache Doris 3.x/4.x（存算一体）的免 SQL 授权管理平台，
> 最终交付为单一二进制，解压即用，支持 统信 UOS V20 / Debian 11.9（linux-x64）。
> 技术栈已确认：Vue 3 + Element Plus + Node.js（Express + mysql2）。

---

## 一、功能清单对照（你的 6 点需求 → 实现位置 → 当前状态）

| # | 你的需求 | 功能实现 | 状态 |
|---|---|---|---|
| 0 | 管理用户账号登录 | 登录页 + JWT 鉴权；平台账号分 admin / readonly 两级，支持多账号管理、改密；首次启动自动生成 admin 初始密码 | ✅ 已实现 |
| 1 | 角色和权限管理：创建角色并授权 | 角色管理页：角色列表（SHOW ROLES）、创建角色+多层级多权限授权、编辑角色授权（diff）、查看角色成员、删除角色（内置 admin/operator 禁删） | ✅ 已实现 |
| 2 | 已创建用户管理（权限、属性） | 用户列表页：按用户名分组、展开显示各来源 host；查看授权明细（SHOW GRANTS 解析为可读表格 + 原始值）、用户属性（SHOW PROPERTY）、重置密码、删除用户 | ✅ 已实现 |
| 3 | 创建用户并授权：权限、来源地址、访问层级（catalog/库/表）、16 位随机强密码 | 创建用户向导：多 host 标签输入（`10.%`、`172.16.%`…）、级联选择 全局→Catalog→库→表（实时从集群拉取，可手填）、权限多选、一键生成 16 位强密码（大小写+数字+特殊字符）、默认角色多选 | ✅ 已实现 |
| 4 | 更新用户授权（同创建项 + 密码） | 编辑授权页：预填当前授权，提交后与 SHOW GRANTS 当前值做 diff，只生成「新增 GRANT + 移除 REVOKE + 改密 ALTER USER」的最小 SQL 集 | ✅ 已实现 |
| 5 | 权限/地址支持多选 | 权限多选框（SELECT/LOAD/ALTER/CREATE/DROP/SHOW_VIEW/GRANT/USAGE/ADMIN，按层级过滤）+「只读 / 读写 / 全量」快捷预设；host 支持任意多个 | ✅ 已实现 |
| 6 | SQL 预览确认 + 自由 SQL | 所有写操作一律先弹 SQL 预览框（编号、等宽、可复制），确认后逐条执行并显示 ✓/✗；独立 SQL 控制台（admin 专属），危险语句二次确认，本地历史回填 | ✅ 已实现 |

**我建议补充且已纳入的功能：**

| 功能 | 说明 | 状态 |
|---|---|---|
| 多集群管理 | 集群增删改、连接实测、顶栏一键切换；连接密码 AES-256-GCM 加密落盘，任何接口不回传明文 | ✅ 已实现 |
| 审计日志 | 每次预览确认执行的 SQL、执行人、集群、成败、错误、IP 全部落 `data/audit.jsonl`，审计页可筛选分页 | ✅ 已实现 |
| 密码强度 | 生成器保证强密码；后端再校验一道（防止弱密码被 Doris 密码策略拒绝） | ✅ 已实现 |
| 登录保护 | 同一 IP 1 分钟失败 5 次锁 5 分钟；JWT 12 小时 | ✅ 已实现 |

**可选增强（未实现，你点头我才做）：**

- 克隆授权：以现有 user@host 为模板一键创建新用户（运维高频场景）
- 用户属性编辑（如 max_user_connections 的 SET PROPERTY）

---

## 二、当前进度盘点（已验证，非纸面）

| 模块 | 内容 | 验证证据 |
|---|---|---|
| 技术规格 | `SPEC.md` 单一事实来源（353 行，含 API 契约、SQL 生成规则、打包方案） | 已落盘 |
| 后端 | 9 个模块约 2500 行：连接池、SQL 生成器、SHOW GRANTS 解析器、鉴权、存储、35+ 个 API 路由 | **31 个单元测试全部通过**（SQL 生成 / 授权解析 / 密码生成，含注入拦截用例） |
| 前端 | 10 个页面 + 2 个共享组件约 2400 行（登录、布局、用户、用户编辑、角色、SQL 控制台、审计、集群、账号） | `vite build` 已成功构建一次 |
| 本地运行 | 后端已启动过一次，自动初始化了 data 目录与 admin 初始密码文件 | `server/data/` 已生成 |

## 三、剩余执行计划（确认后开工）

### 阶段 A — 联调收尾（半天量级）
- [ ] A1 启动前后端联调冒烟：登录 → 添加集群（含不可达地址的报错路径）→ 各页面无 500
- [ ] A2 核对 SPEC 中 UserDetail 独立页：确认其功能已并入用户列表抽屉，或补齐
- [ ] A3 前端最终构建

### 阶段 B — 二进制打包（核心剩余工作）
- [ ] B1 根 `package.json` 编排脚本（dev / build / package / test）
- [ ] B2 `scripts/bundle-server.js`：esbuild 将后端打成单文件 CJS（target node18）
- [ ] B3 `scripts/package.js`：pkg 生成 `node18-linux-x64` 与 `node18-win-x64` 二进制，前端产物内嵌
- [ ] B4 产出交付包 `dorisman-web-v0.1.0-linux-x64.tar.gz`（二进制 + start.sh + 部署 README）

### 阶段 C — 验证与交付
- [ ] C1 Windows 二进制本机冒烟：启动 → 登录页 → 登录 → 各页面无 500
- [ ] C2 验证打包后 data 目录自动创建、初始密码打印、端口可配
- [ ] C3 `README-部署.md`：解压、启动、防火墙、初始密码、Doris 账号要求说明
- [ ] C4 交付清单汇总

---

## 四、需要你确认的 4 个点

1. **计划本身**：上面的功能清单和剩余阶段 A→B→C 是否 OK？有要加/减的功能现在说。
2. **真实 Doris 集群**：目前本机没有可连的 Doris，联调只能验证"连接失败"的错误路径。如果你有一套测试集群（host、9030 端口、admin/root 账号密码）愿意提供，我可以做一轮真实验证（强烈建议，能提前暴露 SHOW GRANTS 在 3.x/4.x 的列格式差异）。
3. **UOS V20 的 CPU 架构**：计划只打 `linux-x64`。如果你的统信机器是 ARM（鲲鹏/飞腾），告诉我，我加打 `linux-arm64`。
4. **打包工具选型**：`pkg` 官方已归档停更，但在 node18 目标上稳定可用，SPEC 也是这么定的。若你更保守，备选方案是"绿色包"形式（tar.gz 内含 Node 运行时 + 应用，同样解压即用，体积略大）。默认我按 pkg 做，打包受阻时自动退到绿色包，不再另行请示。

---

回复"确认"或指出要改的地方，我就按阶段 A→B→C 继续开发。

---

## 五、完成情况（2026-07-17 全部完成）

四个确认点均已按你的回复执行：增强功能全做、真机验证、只打 linux-x64、pkg 简单二进制。

**验证结果（均为实测，非纸面）：**

- 后端单测 **41/41 通过**（含真机样例、注入拦截、SET PROPERTY 生成）
- 真机 E2E **21/21 通过**（`server/tools/e2e-live.js`，对测试集群实连）：
  登录 → 添加集群 →  catalogs/库/用户/角色/授权解析 → 创建测试用户（预览+执行+集群侧直查）
  → 属性编辑（SET PROPERTY 预览+执行+直查）→ 增量授权 diff（只生成 GRANT LOAD）
  → 删除用户 → 审计日志
- 你的集群真实版本：**doris-3.0.6.2-rc01**（经 SHOW FRONTENDS 识别，VERSION() 只返回协议版本 5.7.99）
- Windows 二进制冒烟通过：启动 → 初始密码登录 → 添加集群 → 版本识别 → 元数据拉取

**真机适配修正（SPEC 假设与 3.0.6 实际的差异）：**

1. 权限单元格是裸路径两段式 + 大小写混合（`internal.mysql: Select_priv`），解析器已兼容
2. `SHOW PROPERTY` 不支持 `@host`，已改为按用户名
3. `SHOW CATALOGS` 列名为 `CatalogName`
4. Doris 新用户默认自带 `information_schema` 的 SELECT 授权——编辑授权时请基于预填的当前授权修改，不要清空后重建，否则会把这条系统默认授权 REVOKE 掉
5. E2E 抓到一个真 bug：`/clusters/:id/test` 路由参数名不匹配导致 404，已修复

**交付产物（release/）：**

| 文件 | 大小 | 说明 |
|---|---|---|
| `dorisman-web-v0.1.0-linux-x64.tar.gz` | 18.4 MB | **部署包**：linux 二进制 + start.sh + README-部署.md，拷到 UOS V20 / Debian 11.9 解压即用 |
| `dorisman-linux-x64` | 47.7 MB | linux 单二进制（同 tar.gz 内文件） |
| `dorisman-win-x64.exe` | 39.5 MB | Windows 单二进制（本机调试用） |

linux 二进制与 win 版同代码同机制，但**未在真实 Linux 上启动过**——到目标机后解压先跑一次 `./start.sh` 冒烟（README 有步骤），有问题把控制台输出给我。

**开发期工具（已提交，可复用）：** `server/tools/probe-cluster.js`（只读探测任意集群授权格式）、`server/tools/e2e-live.js`（全链路回归）。



--  26-7-20 新增功能：
1，资源分组管理
2，fe和be节点管理













